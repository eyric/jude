server:
  port: 80
  tomcat:
    uri-encoding: UTF-8
  context-path: /jude
  

spring:
   datasource:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://localhost:3306/jude?characterEncoding=UTF-8
      username: root
      password:
   jpa:
     hibernate.ddl-auto: update
     show-sql: true
   thymeleaf:
    cache: false